<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="umn.ac.id">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StephenSean_00000028167_IF633_FL_UTS">
        <activity android:name=".ProfileOption"></activity>
        <activity android:name=".MulaiLagu" />
        <activity android:name=".ListLagu" />
        <activity android:name=".LoginKuy" />
        <activity android:name=".MainActivity" />
        <activity android:name=".ProfileKu" />
        <activity android:name=".HalamanAwal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

</manifest>